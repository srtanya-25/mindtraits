# personality/views.py
# All REST endpoints for the personality domain are now exposed through the
# `api` app (api/views.py + api/urls.py).
# This file is intentionally empty — kept so Django still sees `personality`
# as a regular app on disk.
